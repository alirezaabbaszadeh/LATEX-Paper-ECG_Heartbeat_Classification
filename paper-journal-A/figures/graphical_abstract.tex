\documentclass{article}
\usepackage[paperwidth=14in,paperheight=6in,margin=0.2in]{geometry}
\usepackage{tikz}
\renewcommand{\familydefault}{\sfdefault}
\usetikzlibrary{arrows.meta, positioning, calc}

\definecolor{darknavy}{HTML}{1B263B}
\definecolor{lightblue}{HTML}{D6E4FF}
\definecolor{lightgreen}{HTML}{E8F5E9}
\definecolor{lightorange}{HTML}{FFF3E0}
\definecolor{lightpurple}{HTML}{F3E5F5}

\pagestyle{empty}
\begin{document}
\begin{tikzpicture}[font=\sffamily, >=latex, node distance=1.1cm]
  \tikzstyle{stage} = [draw=darknavy, rounded corners=3mm, minimum width=4.5cm,
    minimum height=3.8cm, align=center, line width=0.8pt]

  \node[stage, fill=lightblue, text width=4.2cm] (data)
    {\textbf{MIT-BIH Arrhythmia}\\[2pt]
     48 records \\ 110{,}919 beats\\[6pt]
     Patient-level splits\\ Telemonitoring context};

  \node[stage, fill=lightgreen, right=3.8cm of data, text width=4.2cm] (prep)
    {\textbf{Morlet Scalograms}\\[2pt]
     3-beat windows\\R-peak detection\\[6pt]
     Wavelet energy maps\\Metadata tracking};

  \node[stage, fill=lightorange, right=3.8cm of prep, text width=4.2cm] (model)
    {\textbf{Lightweight Conformer}\\[2pt]
     CNN stem + attention\\Hyperband tuned\\[6pt]
     Class-weighted training\\Research\_Runs artifacts};

  \node[stage, fill=lightpurple, right=3.4cm of model, text width=3.6cm] (output)
    {\textbf{Clinical Readout}\\[2pt]
     Accuracy 0.60\\Weighted-F1 0.68\\Macro-F1 0.26\\[6pt]
     Triage abnormal beats};

  \draw[->, line width=1.2pt, darknavy] (data) -- (prep);
  \draw[->, line width=1.2pt, darknavy] (prep) -- (model);
  \draw[->, line width=1.2pt, darknavy] (model) -- (output);

  \node[below=1.3cm of $(prep)!0.5!(model)$, text width=10cm, align=center, darknavy]
    {\textbf{Pipeline:} PhysioNet download $\rightarrow$ Beat windows $\rightarrow$ Morlet scalograms $\rightarrow$ CNN--Conformer $\rightarrow$ Triage-ready metrics};

  \node[below=0.4cm of $(prep)!0.5!(model)$, text width=11cm, align=center, darknavy]
    {Open-source preprocessing, training, and evaluation scripts enable transparency and clinician review.};
\end{tikzpicture}
\end{document}
